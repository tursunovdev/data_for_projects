def find_most_expensive_product(product_data):
    """
    Find the most expensive product.

    Args:
        product_data (list): A list of lists representing product data.

    Returns:
        list: A list representing the most expensive product (name and price).
    """