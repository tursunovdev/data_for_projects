def filter_products_by_category(product_data, category):
    """
    Filter products by category and return a new list of lists containing only the products from the specified category.

    Args:
        product_data (list): A list of lists representing product data.
        category (str): The category to filter by.

    Returns:
        list: A filtered list of lists containing products from the specified category.
    """