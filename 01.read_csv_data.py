import csv

def read_csv_data(filename):
    """
    Read the CSV file and return a list of lists representing each row of data.

    Args:
        filename (str): The name of the CSV file.

    Returns:
        list: A list of lists representing the CSV data.
    """
    